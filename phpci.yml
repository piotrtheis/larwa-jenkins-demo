build_settings:
    ignore:
        - "vendor"
        - "tests"

setup:
    composer:
        action: "install"

test:
    php_unit:
        config:
            - "phpunit.xml"
        path: "tests/"
    php_mess_detector:
        path: 'app'
        ignore:
          - 'vendor'
        rules:
          - "cleancode"
          - "controversial"
          - "codesize"
          - "design"
          - "naming"
          - "unusedcode"
    php_code_sniffer:
        path: "app"
        standard: "PSR2"
    php_cpd:
        standard: "PSR2"
        path: "app"
